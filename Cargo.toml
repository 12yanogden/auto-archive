[package]
name = "auto-archive"
version = "0.2.1"
edition = "2021"
repository = "https://github.com/12yanogden/auto-archive"

[dependencies]
tar = "0.4"
flate2 = "1.0"
chrono = "0.4"

[profile.release]
lto = true
strip = true

[profile.dist]
inherits = "release"
lto = "thin"
